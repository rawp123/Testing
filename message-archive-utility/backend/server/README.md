# Backend Server Package

This folder is the FastAPI server package for Message Archive Utility.

- `main.py`: API routes, database initialization, local auth, and response shaping.
- `db/`: SQLite schema owned by this backend.
- `importers/`: source-specific message import logic.
- `services/`: search, export, contact display, and message normalization helpers.
